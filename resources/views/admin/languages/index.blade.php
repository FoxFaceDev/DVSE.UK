<x-layouts.admin title="Languages"><div class="mb-5 flex justify-between"><div>@if(session('success'))<p class="rounded bg-green-50 p-3 text-green-700">{{ session('success') }}</p>@endif</div><a href="{{ route('admin.languages.create') }}" class="rounded bg-primary px-4 py-2 text-white">Add language</a></div><div class="overflow-hidden rounded-lg border bg-white"><table class="w-full text-left"><thead class="bg-gray-50"><tr><th class="p-4">Language</th><th>Code</th><th>Direction</th><th>Status</th><th class="p-4 text-right">Actions</th></tr></thead><tbody>@foreach($languages as $language)<tr class="border-t"><td class="p-4 font-bold">{{ $language->name }}</td><td>{{ $language->code }}</td><td>{{ strtoupper($language->direction) }}</td><td>{{ $language->is_active ? 'Active' : 'Inactive' }}</td><td class="p-4 text-right"><a href="{{ route('admin.languages.edit',$language) }}" class="text-primary">Edit</a>@if($language->code !== 'en')<form method="POST" action="{{ route('admin.languages.destroy',$language) }}" class="ml-3 inline" onsubmit="return confirm('Delete this language?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form>@endif</td></tr>@endforeach</tbody></table></div></x-layouts.admin>
