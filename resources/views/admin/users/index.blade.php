<x-layouts.admin title="User Accounts"><form class="mb-5 flex gap-2"><input name="q" value="{{ request('q') }}" placeholder="Search name or email" class="rounded border-gray-300"><button class="rounded bg-primary px-4 text-white">Search</button></form>@if(session('success'))<p class="mb-4 rounded bg-green-50 p-3 text-green-700">{{ session('success') }}</p>@endif<div class="overflow-hidden rounded-lg border bg-white"><table class="w-full text-left"><thead class="bg-gray-50"><tr><th class="p-4">Name</th><th>Email</th><th>Type</th><th class="p-4 text-right">Actions</th></tr></thead><tbody>@forelse($users as $user)<tr class="border-t"><td class="p-4">{{ $user->name }}</td><td>{{ $user->email }}</td><td>{{ ucfirst($user->account_type) }}</td><td class="p-4 text-right"><a class="text-primary" href="{{ route('admin.users.edit',$user) }}">Edit / reset password</a><form class="ml-3 inline" method="POST" action="{{ route('admin.users.destroy',$user) }}" onsubmit="return confirm('Permanently delete this user?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-gray-500">No users found.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $users->links() }}</div></x-layouts.admin>
