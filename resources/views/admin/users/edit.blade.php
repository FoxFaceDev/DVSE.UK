<x-layouts.admin title="Edit User"><div class="mx-auto max-w-2xl space-y-6">@if(session('success'))<p class="rounded bg-green-50 p-3 text-green-700">{{ session('success') }}</p>@endif<form method="POST" action="{{ route('admin.users.update',$user) }}" class="space-y-4 rounded-lg border bg-white p-6">@csrf @method('PUT')<h3 class="font-bold">Account details</h3><input name="name" required value="{{ old('name',$user->name) }}" class="w-full rounded border-gray-300"><input type="email" name="email" required value="{{ old('email',$user->email) }}" class="w-full rounded border-gray-300"><select name="account_type" class="w-full rounded border-gray-300"><option value="user" @selected($user->account_type==='user')>User</option><option value="instructor" @selected($user->account_type==='instructor')>Instructor</option></select><button class="rounded bg-primary px-4 py-2 text-white">Save details</button></form><form method="POST" action="{{ route('admin.users.reset-password',$user) }}" class="space-y-4 rounded-lg border bg-white p-6">@csrf @method('PUT')<h3 class="font-bold">Reset password</h3><input type="password" name="password" required placeholder="New password" class="w-full rounded border-gray-300"><input type="password" name="password_confirmation" required placeholder="Confirm new password" class="w-full rounded border-gray-300"><button class="rounded bg-amber-600 px-4 py-2 text-white">Reset password</button></form></div></x-layouts.admin>
